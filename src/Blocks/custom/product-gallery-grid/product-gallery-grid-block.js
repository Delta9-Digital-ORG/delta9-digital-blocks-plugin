import React from 'react';
import { ProductGalleryGridEditor } from './components/product-gallery-grid-editor';

export const ProductGalleryGrid = (props) => {
	return (
		<ProductGalleryGridEditor {...props} />
	);
};
